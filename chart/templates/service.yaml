apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name }}-svc
  labels:
    app: {{ .Release.Name }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.port }}
      targetPort: {{ .Values.target_port }}
      protocol: TCP
  selector:
    app: {{ .Release.Name }}
